"use server";

// Server Actions untuk portfolio
// Contoh: getPortfolios, createPortfolio, updatePortfolio, deletePortfolio, etc.

export async function getPortfolios(userId: string) {
  // Implementasi di sini
}

export async function createPortfolio(userId: string, data: unknown) {
  // Implementasi di sini
}
